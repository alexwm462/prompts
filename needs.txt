one script to set up the project
    - 

create_ptoject.sh script
    - add a snippet injection into the head of the SUPABASE_URL and SUPABASE_ANON_KEY

bash script rules
    - idempotency
    - no exit 1 statements - should be able to be pasted into the terminal directly
    - if the user hasn't input something, ask for it and try again
    - where possible do things completely automated. where not possible, ask for user input
    - 

netlify serverless functions
    - add detailed logging to each functions

supabase migrations
    - prefix any table that you make with the project_name

script.js
    - get netlify to inject any env vars into the code. This required 